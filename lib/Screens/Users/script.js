// Define your variable
var myVariable = "Hello, World!";

// Get a reference to the HTML element where you want to display the variable
var displayElement = document.getElementById("variableDisplay");

// Update the content of the HTML element with your variable
displayElement.textContent = myVariable;
